<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.akshatsharma.donationapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DonationApp">
        <activity android:name=".MyDonationsActivity"></activity>
        <activity android:name=".ViewDonationActivity" />
        <activity android:name=".DisplayAllDonationsActivity" />
        <activity android:name=".AuctionDetailsActivity" />
        <activity android:name=".DonationDetailsActivity" />
        <activity android:name=".AddAuctionActivity" />
        <activity android:name=".AddDonationActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>